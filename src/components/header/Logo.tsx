import React from 'react';

import { Logo as LogoContainer } from './styles';

const Logo: React.FC = () => {
  return (
    <LogoContainer>
      <span>Wookie</span>
      <span>Moovies</span>
    </LogoContainer>
  );
};

export default Logo;
